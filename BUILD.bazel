<<<<<<< HEAD
package(default_visibility = ["//visibility:public"])

exports_files(["WORKSPACE"])

# These package groups mirror the root labels Envoy's Bazel macros expect.
# Keeping them here avoids visibility and repository-config failures when
# consuming Envoy's build system from this workspace.
package_group(
    name = "extension_config",
    packages = [
        "//src/...",
        "//test/...",
    ],
)

package_group(
    name = "extension_library",
    packages = [
        "//src/...",
        "//test/...",
    ],
)

package_group(
    name = "contrib_library",
    packages = [],
)

package_group(
    name = "examples_library",
    packages = [],
)

package_group(
    name = "mobile_library",
    packages = [],
)
=======
# Root BUILD.bazel
# Required by Bazel to recognize the repo root as a package.
# envoy_extension_package() in sub-packages references //:mobile_library
# which is declared here by envoy_extension_package() at root level.

load(
    "@envoy//bazel:envoy_build_system.bzl",
    "envoy_cc_library",
    "envoy_extension_package",
)

licenses(["notice"])  # Apache 2

envoy_extension_package()
>>>>>>> d920cdd3ae0bc2aaac9bd8c72444e9acef01443c
